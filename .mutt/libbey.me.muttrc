# vim:ft=muttrc

set spoolfile = "+p:INBOX"
set mbox      = "+p:archive"
set postponed = "+p:drafts"
set from      = "burke@libbey.me"
# no idea why sudo is required when built with nix.
#set sendmail  = "sudo /Users/burke/.nix-profile/bin/msmtp -C /Users/burke/.msmtprc -a burkelibbey"
set sendmail  = "/usr/local/bin/msmtp -C /Users/burke/.msmtprc -a burkelibbey"
