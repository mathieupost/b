# vim:ft=muttrc

set spoolfile = "+s:INBOX"
set mbox      = "+s:archive"
set postponed = "+s:drafts"
set from      = "burke.libbey@shopify.com"
# no idea why sudo is required when built with nix.
#set sendmail  = "sudo /Users/burke/.nix-profile/bin/msmtp -C /Users/burke/.msmtprc -a shopify"
set sendmail  = "/usr/local/bin/msmtp -C /Users/burke/.msmtprc -a burkelibbey"
